import React from "react";

export default function WriteButton() {
  return (
    <button
      style={{
        width: "100%",
        padding: "8px 0",
        backgroundColor: "gray",
        color: "#fff",
        border: "none",
        borderRadius: "4px",
        cursor: "pointer",
      }}
    >
      글쓰기
    </button>
  );
}
